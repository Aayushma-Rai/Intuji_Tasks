# Blog API and Logic Tasks

This project contains a backend API for a Blog system and a Python solution for the "Two-Sum" array logic problem. 



### Prerequisites
* Python 3.14.0 installed.
* Flask library installed.

### Installation
1. Clone the repository or download the files.
2. Install the required dependencies:
   ```bash
   pip install flask